# Samar — Dev Activity Log (Branch: `samar`)

- **2026-08-14**: Implemented complete PySide6 desktop UI (MainWindow, 5 views, recorder with live waveform, transcript editor, AI tasks, semantic search, analytics, export & profile dialogs, and Retro Warm Cream QSS design system).
- **2026-08-14**: Added UI preview screenshots for all primary application screens to `assets/`.
- **2026-08-15**: Created developer documentation and tracking log in `doc/dev/samar.md`.
- **2026-08-15**: Created `requirements.txt` with all runtime, GUI, AI, and database dependencies.
- **2026-08-19**: Designed new functional UI system with StitchMCP (`VoiceNote AI Desktop App` / project ID `3607827371018100199`).
- **2026-08-19**: Overhauled complete desktop UI into a modern Dark Bento Grid design system (`styles.py`) with deep slate canvas, neon glowing accents, and high-readability typography.
- **2026-08-19**: Upgraded `AudioRecorderWidget` with live streaming Whisper STT preview, dynamic neon waveform visualizer, input device selector, and AI noise suppression toggle.
- **2026-08-19**: Enhanced `TranscriptViewWidget` with interactive audio scrubber timeline, playback speed multiplier (1.0x - 2.0x), multi-speaker dialogue blocks, confidence score, and topic tag manager.
- **2026-08-19**: Upgraded `SummaryTaskWidget` with executive summaries, interactive action item checklist (priority badges, assignees, due dates), and ChromaDB semantic cross-references.
- **2026-08-19**: Verified full integration with the existing architecture and pipeline workers; validated UI views and components with test suite (`run_all_tests.py`) and live application launch (`python main.py`).
- **2026-08-19**: Re-aligned and calibrated full PySide6 styling (`styles.py`) to match the Retro Warm Cream design palette from `assets/` mockups.
- **2026-08-19**: Refactored `AudioRecorderWidget` state transitions: fixed Pause/Resume toggle flow, added permanent session lock and control disabling on Stop Transcribe, and added unbuffered real-time standard output logging in `main.py`.
- **2026-08-21**: Designed and implemented the database-backed **Login and Registration System** (`LoginDialog` in `voicenote/ui/dialogs/login_dialog.py`) with PostgreSQL persistence, unique user/email validation, and SHA-256 password hashing.
- **2026-08-21**: Redesigned the Login portal into a full-window (`1280x840`) dual-panel **Bento Grid UI** matching the Home page aesthetics (`#ECE7DF` warm cream canvas, studio feature showcase, and interactive auth cards).
- **2026-08-21**: Integrated user session authentication flow into `main.py` and `main_window.py`, including dynamic user display name propagation in `HeaderWidget` and interactive **"Sign Out of Account"** in `ProfileDialog`.
- **2026-08-21**: Streamlined `HeaderWidget` by removing legacy status badges for a clean search & profile layout, and updated `requirements.txt` with active PostgreSQL database dependencies.
- **2026-08-21**: Built automated unit testing suite for user authentication flows (`tests/test_auth.py`) and verified all 13 test suites.
- **2026-08-21**: Configured and implemented centralized audio recording storage in `data/recording/` folder (`RECORDING_DIR` in `voicenote/config.py`). Updated `AudioRecorderWidget` to serialize captured microphone audio sessions as PCM WAV files (`recording_YYYYMMDD_HHMMSS.wav`) and copy imported audio files directly into `data/recording/`, updated `MainWindow` pipeline workers to bind file paths to database records, and added automated storage test suites (`tests/test_audio_recording.py`).
- **2026-08-21**: Implemented the live hardware microphone audio recording engine (`AudioEngine` in `voicenote/core/audio_engine.py`) using `sounddevice` and `numpy`. Integrated dynamic input device enumeration into `AudioRecorderWidget`, real-time PCM audio streaming, live voice amplitude tracking for `WaveformWidget`, and 16-bit 16kHz PCM WAV recording serialization to `data/recording/`. Enabled `sounddevice`, `soundfile`, and `numpy` in `requirements.txt` and verified with comprehensive test runner.
- **2026-08-24**: Built the multi-format **Export Engine** (`ExportEngine` in `voicenote/core/export_engine.py`) supporting professional **PDF** generation via ReportLab (custom palettes, metadata headers, executive summary callout blocks, styled action item tables, and speaker timestamp transcripts), **Word (DOCX)** document generation via `python-docx` (custom styles, structured checklists, and dialogue blocks), and **Plain Text (TXT)** formatted exports. Upgraded `ExportDialog` with native save file destination browsing, dynamic extension switching, section toggles (summary, tasks, transcripts, metadata), and instant "Open Document / Open Folder" actions. Connected `MainWindow` note cards and sidebar export buttons to load full database relational note context (`Note`, `Transcript`, `AISummary`, `Tasks`), enabled `reportlab` & `python-docx` in `requirements.txt`, built 7 automated unit test suites (`tests/test_export_engine.py`), and verified 100% test pass rate in `run_all_tests.py`.
- **2026-08-24**: Fixed Speech-to-Text (STT) transcription and AI analysis pipeline issues: (1) Removed hardcoded `language="mr"` in `stt_engine.py` to enable automatic multi-language detection (English, Hindi, Marathi, etc.), (2) Updated invalid Gemini model identifier in `ai_engine.py` to `gemini-2.5-flash` to resolve HTTP 400 Bad Request error, (3) Enhanced `PipelineWorker` with audio duration calculation, timestamped segment formatting, and robust Groq-to-Whisper fallback, and (4) Updated `TranscriptViewWidget`, `SummaryTaskWidget`, and `MainWindow` with dynamic data rendering methods (`set_note_transcript`, `set_ai_data`) to display real transcription outputs upon note selection and recording completion.
- **2026-08-25**: Implemented the core **Analytics Engine** (`AnalyticsEngine` in `voicenote/core/analytics_engine.py`) for note duration parsing, weekly recording activity aggregation, task completion statistics, AI time savings calculations, and topic tag frequency extraction. Upgraded `AnalyticsDashboardWidget` to dynamically render real database insights with interactive manual refresh. Designed and implemented the multi-tab **Settings Dialog** (`SettingsDialog` in `voicenote/ui/dialogs/settings_dialog.py`) for microphone hardware selection, STT/LLM provider toggles, local recording storage folder access, and database diagnostics. Wired sidebar settings triggers and auto-refresh mechanisms in `MainWindow`, created automated unit tests in `tests/test_analytics_engine.py`, and verified 100% pass rate in `run_all_tests.py`. Added single recording **"Remove"** and **"Remove All Recordings"** bulk purge features with disk audio cleanup and cascading PostgreSQL relational deletion.
