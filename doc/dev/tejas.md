# Tejas — Dev Activity Log (Branch: `tejas`)

- **2026-08-18** | **Task:** Updated application configuration constants (`APP_NAME`, `APP_SUBTITLE`, `VERSION`, storage directories `DATA_DIR`, `RECORDINGS_DIR`, `DB_PATH`) in `voicenote/config.py`.
- **2026-08-18** | **Task:** Built thread-safe SQLite database persistence layer (`voicenote/db/models.py`, `voicenote/db/database.py`, `voicenote/db/__init__.py`) supporting `Note`, `Transcript`, `AISummary`, and `Task` relational models and initial sample seeding.
- **2026-08-18** | **Task:** Implemented PySide6 background worker thread service (`voicenote/services/worker.py`) using `QThread` and Qt signals (`progress`, `finished`, `error`) for non-blocking speech-to-text (Whisper/Groq) and Gemini LLM analysis.
- **2026-08-18** | **Task:** Integrated `MainWindow` (`voicenote/ui/main_window.py`) with SQLite database manager for dynamic note rendering, live audio recording pipeline callbacks, and status bar updates.
- **2026-08-18** | **Task:** Created application bootstrap entry point (`main.py`) to initialize PySide6 `QApplication`, database connections, and launch `MainWindow`.
- **2026-08-18** | **Task:** Refactored persistence layer (`voicenote/db/database.py`) to use PostgreSQL (`psycopg2`) as the primary database engine with auto-table schema creation and relational cascading constraints.
- **2026-08-18** | **Task:** Created `.env.example` detailing PostgreSQL connection credentials (`DB_ENGINE`, `POSTGRES_HOST`, `POSTGRES_PORT`, `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`) and AI API keys (`GEMINI_API_KEY`, `GROQ_API_KEY`).
- **2026-08-18** | **Task:** Authored system architecture specification (`doc/architecture.md`) covering PostgreSQL (relational metadata), ChromaDB (vector embeddings), and File Storage (audio recordings).
- **2026-08-18** | **Task:** Verified application pipeline using `run_all_tests.py` and committed/pushed all changes to branch `tejas`.
- **2026-08-19** | **Task:** Configured strictly PostgreSQL storage engine (`DatabaseManager` in `voicenote/db/database.py`) with automated database creation (`CREATE DATABASE voicenote`), relational schema initialization, sample data seeder, and unit test suite (`tests/test_db.py`).
- **2026-08-20** | **Task:** Added `users` table, `User` data model, SHA-256 password hashing, authentication methods (`create_user`, `verify_user_login`), default demo user (`admin`/`admin123`), and comprehensive `doc/database-setup.md` documentation.
- **2026-08-25** | **Task:** Integrated ChromaDB vector store (`VectorEngine`) into background `PipelineWorker` (`voicenote/services/worker.py`) to automatically index timestamped transcript segments and metadata upon recording completion.
- **2026-08-25** | **Task:** Wired real-time vector similarity search into `SemanticSearchWidget` (`voicenote/ui/components/semantic_search_widget.py`) to execute live ChromaDB similarity queries with distance-to-percentage scoring and timestamp jumps.
- **2026-08-25** | **Task:** Added `run_vector_engine_tests()` runner function to `run_all_tests.py` to cover ChromaDB vector store unit tests across all 8 core system modules.
- **2026-09-19** | **Task:** (Phase 4 Quality Gates) Implemented `delete_all()` in `VectorEngine` (`voicenote/core/vector_engine.py`) and synchronized note removal workflows in `MainWindow` (`on_delete_note`, `on_delete_all_notes`) across PostgreSQL, disk audio recordings, and ChromaDB vector embeddings.
- **2026-09-19** | **Task:** (Phase 4 Quality Gates) Added `get_note_by_title` to `DatabaseManager` (`voicenote/db/database.py`) and added unit test in `tests/test_db.py`.
- **2026-09-19** | **Task:** (Phase 4 Quality Gates) Enhanced `SemanticSearchWidget` with `reset()` to automatically clear search views upon note deletion and prevent orphan search results.
- **2026-09-19** | **Task:** (Phase 4 Quality Gates) Hardened `ProcessingDialog` cleanup in `MainWindow.on_pipeline_error` for non-blocking UI resilience during failed or cancelled AI pipelines.
- **2026-09-19** | **Task:** (Phase 4 Quality Gates) Added automated database reseeding in `run_all_tests.py` and successfully passed 100% of the 9 core test suites. Note: Phase 5 packaging is held per mentor direction until additional features and real-world testing are concluded.
- **2026-09-22** | **Task:** Upgraded AI engine configuration to support Gemini `gemini-3.8-flash` with multi-tier fallback resilience in `voicenote/core/ai_engine.py` against service spikes and quota limits.
- **2026-09-22** | **Task:** Built `TranscriptCorrector` (`voicenote/core/transcript_corrector.py`) using Gemini AI with Devanagari correction rules for Marathi/Hindi, resolving Whisper grammar confusion, restoring mistranslated phrases, preserving English business terms, and standardizing currency (Rs.) and percentage (%) figures.
- **2026-09-22** | **Task:** Integrated `TranscriptCorrector` into `PipelineWorker` (`voicenote/services/worker.py`) at Step 1.5, feeding clean text to both `DiarizationEngine` and `AIEngine.analyze_transcript`.
- **2026-09-22** | **Task:** Built real-time audio capture and monitoring telemetry in `AudioEngine` (`voicenote/core/audio_engine.py`) calculating RMS amplitude, peak dBFS levels, and dynamic device switching for microphone and system loopback.
- **2026-09-22** | **Task:** Upgraded `WaveformWidget` and `AudioRecorderWidget` with live pre-recording audio calibration test mode, RMS meter bars, dynamic device dropdown refresh, and live status badges.
- **2026-09-22** | **Task:** Integrated Lucide SVG vector icon library (`assets/icons/`) and `IconHelper` (`voicenote/ui/icon_helper.py`) providing resolution-independent UI icons across recorder modes, device selectors, and action controls.
- **2026-09-22** | **Task:** Embedded Noto Sans Devanagari and Nirmala UI TrueType font families in `assets/fonts/` for native Devanagari script rendering.
- **2026-09-22** | **Task:** Overhauled `ExportEngine` (`voicenote/core/export_engine.py`) to support multi-lingual Marathi, Hindi, and English PDF exports with font fallback, Devanagari word shaping, clean table structures, and executive summary formatting.
- **2026-09-22** | **Task:** Eliminated harsh wireframe rectangular box lines across all UI components (`AnalyticsDashboardWidget`, `SummaryTaskWidget`, `MainWindow`, `TranscriptViewWidget`, `SemanticSearchWidget`, `LoginDialog`) by implementing a global `QLabel` border reset in `voicenote/ui/styles.py` and scoping container QSS rules.
- **2026-09-22** | **Task:** Redesigned all status tags, topic chips, suggestion buttons, and overdue indicators into clean, borderless soft-tinted pills with rounded corners.
- **2026-09-22** | **Task:** Cleaned up project repository, updated `.gitignore` for runtime ChromaDB and scratch directories, and verified all unit and integration test suites.

