# Commit Activity Logging Rule

Whenever committing changes to the `samar` branch:
1. Automatically update `doc/dev/samar.md` before committing.
2. Add a new bullet point with the current date (YYYY-MM-DD) and a concise one-line summary of the activity/changes made in that commit.
3. Keep the file concise and chronologically ordered.
